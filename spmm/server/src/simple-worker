// vim:set ft=cpp:

#pragma once

#include <cstdio>

#include "worker"

namespace Spmm
{

class SimpleWorker : public Worker
{
public:
  void run(void) override
  {
    printf("worker run...\n");
    this->core->pfree(this, 1);
    while(1);
  }

  void pinc_refcnt(l4_addr_t p) override
  {

  }

  void pdec_refcnt(l4_addr_t p) override
  {

  }
};

} //Spmm
